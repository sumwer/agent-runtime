[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    legacy: historical real Opik integration tests (opt in with RUN_LEGACY_OPIK=1)
    e2e: real model integration (opt in with RUN_LLM_E2E=1)
